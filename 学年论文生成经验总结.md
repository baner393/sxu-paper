# 学年论文 Word 文档生成 — 经验总结

## 总体思路

使用 C# + OpenXML SDK 3.2.0 直接生成符合山西财经大学学年论文规范的 Word 文档。不依赖模板文件，完全从零构建。

### 为什么用 C# 直接写而不是用 CLI 或模板

- 文档需要多个节（section），每个节有不同的页眉页脚规则
- 需要三线表、脚注、图片、TOC 等复杂元素
- CLI 工具和模板方式无法精细控制这些元素的组合
- OpenXML SDK 提供完整的 API，可以精确控制每个 XML 元素

## 文档架构设计

### 最终节结构（7节，经验证可行）

```
第1节（封面+说明+学术承诺）    第2节（摘要+英文摘要+目录）  第3-7节（正文→封底）
┌─────────────────────┐  ┌─────────────────────┐  ┌─────────────────────┐
│ 无页眉              │  │ 页眉奇右/偶左       │  │ 页眉奇右/偶左       │
│ 无页码              │  │ 大写罗马数字页码      │  │ 阿拉伯数字页码      │
│ 3空白页，无页眉页脚   │  │ 3页用分页符分隔      │  │ 奇偶页不同          │
│                     │  │ 奇偶页不同 Ⅰ,Ⅱ,Ⅲ    │  │ 脚注每页重新编号     │
│ 通过 sectPr          │  │ 通过 sectPr          │  │ 最终 sectPr          │
│ (在段落 pPr 中)      │  │ (在段落 pPr 中)      │  │ (在 body 末尾)       │
└─────────────────────┘  └─────────────────────┘  └─────────────────────┘
```

**节结构关键规则**：
- 前 6 节的 `sectPr` 放在该节最后一个段落的 `pPr` 中
- 最终节的 `sectPr` 放在 `body` 的最后一个子元素
- **单页节必须合并**：中文摘要、英文摘要、目录各只有1页，必须合并为**同一个节**（内部用分页符 `Break{Type=Page}` 分隔），否则单页节无法体现奇偶页差异，且罗马数字页码容易错乱

### 页眉页脚实现

```csharp
// 每个页眉/页脚是独立的 Part
var headerPart = mainPart.AddNewPart<HeaderPart>();
headerPart.Header = new Header(/* 段落 + 文本 */);

// 通过 HeaderReference/FooterReference 关联到 sectPr
sectPr.Append(new HeaderReference { 
    Type = HeaderFooterValues.Default,  // 奇数页（默认）
    Id = mainPart.GetIdOfPart(headerPart) 
});
```

**奇偶页不同必须同时设置两样东西**：
1. `sectPr` 中必须包含 `new EvenAndOddHeaders()` — 这是启用的开关
2. 必须同时添加 `Type=Default`（奇数页）和 `Type=Even`（偶数页）的 HeaderReference/FooterReference

## 遇到的问题与解决方案

### 1. 中文引号导致 C# 编译错误

**现象**：文本中的 `"刷不停"` 和 `"更新域"` 等中文引号 `""` 被 C# 编译器误解析。

**原因**：中文弯引号 `"` (U+201C) 和 `"` (U+201D) 在某些编码下被当作 ASCII 双引号 `"` (U+0022)，导致字符串提前结束。

**解决**：替换为中文全角引号 `"“"` 和 `"”"`，确保是明确的 Unicode 字符。

### 2. OpenXML SDK 没有 `GetOrCreate<T>()` 方法

**现象**：`Paragraph.GetOrCreate<ParagraphProperties>()` 编译失败。

**原因**：OpenXML SDK 的 `OpenXmlElement` 没有这个方法（这是某些扩展库提供的）。

**解决**：手动检查并创建：
```csharp
var pp = p.Elements<ParagraphProperties>().FirstOrDefault();
if (pp == null) { pp = new ParagraphProperties(); p.PrependChild(pp); }
```

### 3. `Footnote.Id.Value` 返回 `long` 而非 `int`

**现象**：`fnId = fn.Id.Value + 1` 编译报错 CS0266（long→int 隐式转换）。

**解决**：显式转换 `fnId = (int)fn.Id.Value + 1`。

### 4. `NumberingRestart.Val` 需要枚举值而非整数

**现象**：`new NumberingRestart { Val = 2 }` 编译报错 CS0029。

**原因**：`Val` 属性的类型是 `EnumValue<RestartNumberValues>`，不接受裸 `int`。

**解决**：使用枚举 `new NumberingRestart { Val = RestartNumberValues.EachPage }`。
可选值：`Continuous`、`EachSection`、`EachPage`。

### 5. TOC 无法提取标题 — OutlineLevel 是必须的

**现象**：Word 中更新 TOC 域后目录为空。

**原因**：TOC 的 `\o` 开关按段落的 **OutlineLevel** 提取标题。仅定义 Heading 样式但段落不引用该样式、或不设置 OutlineLevel，TOC 就无法识别。

**解决**：在每个标题段落的 `ParagraphProperties` 中显式添加：
```csharp
pp.Append(new OutlineLevel { Val = 0 });  // 0=一级, 1=二级, 2=三级
```

**TOC 级别映射**：
| OpenXML OutlineLevel.Val | TOC 级别 | 对应标题 |
|---|---|---|
| 0 | Level 1 | 一级标题（黑体三号） |
| 1 | Level 2 | 二级标题（宋体四号加粗） |
| 2 | Level 3 | 三级标题（不显示在目录） |

### 6. Drawing 命名空间与 Wordprocessing 命名空间冲突（2026-05-20 新增）

**现象**：添加 `using DocumentFormat.OpenXml.Drawing;` 后，大量 CS0104 错误："RunProperties"是"Drawing.RunProperties"和"Wordprocessing.RunProperties"之间的不明确的引用。

**原因**：`DocumentFormat.OpenXml.Drawing` 和 `DocumentFormat.OpenXml.Wordprocessing` 都定义了同名的类（RunProperties、Paragraph、ParagraphProperties 等）。

**解决**：使用 using 别名，避免将 Drawing 命名空间导入全局：
```csharp
using A = DocumentFormat.OpenXml.Drawing;
using DW = DocumentFormat.OpenXml.Drawing.Wordprocessing;
using PIC = DocumentFormat.OpenXml.Drawing.Pictures;
```
然后所有 Drawing 相关类型使用前缀：`A.Blip`、`DW.Inline`、`PIC.Picture` 等。

**注意**：`GraphicFrameLocks` 在 `DocumentFormat.OpenXml.Drawing`（`A.GraphicFrameLocks`），不在 `.Drawing.Wordprocessing` 中。

### 7. Em-dash 和右单引号也可能导致编译问题（2026-05-20 新增）

**现象**：U+2014（—，em dash）和 U+2019（'，right single quotation mark）在 C# 字符串中可能导致编译失败。

**解决**：全部替换为 C# Unicode 转义序列 `—` 和 `’`。C# 源码文件使用无 BOM 的 UTF-8 编码。最关键的是避免 U+201C/U+201D/U+2014/U+2019 等可能被误解析的 Unicode 标点符号直接出现在源码中。

### 8. 奇偶页页眉页脚不生效（2026-05-20 新增）

**现象**：虽然 sectPr 中添加了 Default 和 Even 两套 HeaderReference/FooterReference，但在 Word 中奇偶页页眉页脚没有区分。

**原因**：缺少 `EvenAndOddHeaders` 元素。只有同时满足两个条件 Word 才会启用奇偶页不同：
1. `sectPr` 中包含 `<w:evenAndOddHeaders/>`（即 `new EvenAndOddHeaders()`）
2. 同时存在 `Type=Default` 和 `Type=Even` 的 HeaderReference/FooterReference

**解决**：在 `MakeArabicSectPr` 方法中添加：
```csharp
sp.Append(new EvenAndOddHeaders());
```

### 9. 中文摘要/英文摘要/目录的页眉也需要奇偶页不同（2026-05-20 新增）

**现象**：最初只给正文区域（第5-9节）设置了奇偶页不同的页眉页脚，但摘要和目录部分（第2-4节）只用了居中页眉。

**解决**：第2-4节也使用与正文相同的 odd/even 页眉页脚部件，只是页码格式改为 `LowerRoman`。这样整个文档（除封面区域外）的页眉页脚风格统一：奇数页右对齐、偶数页左对齐。

### 10. 文件被占用导致生成失败（2026-05-20 新增）

**现象**：`System.IO.IOException: The process cannot access the file ... because it is being used by another process.`

**原因**：Word 打开文档时会锁定文件，此时无法覆盖写入。

**解决**：生成前确保在 Word 中关闭目标文档。

### 11. 单页节无法体现奇偶页差异 & 罗马数字错乱（2026-05-20 新增，核心经验）

**现象**：中文摘要、英文摘要、目录各占1页，虽然每节的 sectPr 都正确设置了 EvenAndOddHeaders + Default/Even 两套 HeaderReference/FooterReference，但在 Word 中页眉始终一个对齐方式，罗马数字页码Ⅰ、Ⅱ、Ⅲ也不连续。

**原因**：每个节只有 1 页（全是奇数页），Word 不会在同一节内切换奇偶页眉。同时三个独立的节之间页码编号可能因 SectionType（OddPage/NextPage）的空白页插入而错乱。

**解决**：将中文摘要、英文摘要、目录**合并为同一个节**（Section），内部用**分页符**（`new Break { Type = BreakValues.Page }`）而非分节符分隔。合并后该节有 3 页，奇偶页差异立即生效，罗马数字Ⅰ、Ⅱ、Ⅲ也自然连续。

```csharp
// 合并为1个节，内部用分页符
b.Append(/* 中文摘要内容 */);
H.AddPageBreak(b);  // 分页符，不是分节符
b.Append(/* 英文摘要内容 */);
H.AddPageBreak(b);
b.Append(/* 目录内容 */);
H.CloseSection(b, H.MakeArabicSectPr(..., NumberFormatValues.UpperRoman, 1));
```

**AddPageBreak 实现**（在上一段的末尾追加分页符）：
```csharp
public static void AddPageBreak(Body body)
{
    var p = body.Elements<Paragraph>().LastOrDefault();
    if (p == null) return;
    p.Append(new Run(new Break { Type = BreakValues.Page }));
}
```

### 12. sectPr 子元素顺序错误导致 EvenAndOddHeaders 不生效（2026-05-20 新增）

**现象**：sectPr 中包含了 EvenAndOddHeaders 和 Default/Even 两套 HeaderReference/FooterReference，但 Word 中奇偶页仍不区分。

**原因**：OpenXML 对 sectPr 的子元素有严格的顺序要求。错误的顺序会导致 Word 静默忽略某些元素。

**正确的 sectPr 子元素顺序**（经 Word 验证）：
```
headerRef* → footerRef* → [type] → pgSz → pgMar → docGrid → pgNumType → evenAndOddHeaders
```

**解决**：SectionType（`<w:type>`）必须在 pgSz 之前，EvenAndOddHeaders 在最后。所有 headerReference/footerReference 必须在最前面。务必在 MakeArabicSectPr 内按此顺序一次性构建，不要在外部追加 SectionType 或 FootnoteProperties。

## 关键数值速查

### 字号对照
| 字号 | pt | w:sz |
|------|-----|------|
| 小二 | 18 | 36 |
| 三号 | 16 | 32 |
| 四号 | 14 | 28 |
| 小四 | 12 | 24 |
| 五号 | 10.5 | 21 |
| 小五 | 9 | 18 |

### 页边距
| 边距 | cm | DXA |
|------|-----|-----|
| 上 | 3.0 | 1701 |
| 下 | 2.5 | 1418 |
| 左 | 2.5 | 1418 |
| 右 | 2.0 | 1134 |

### 行距与缩进
| 设置 | 值 |
|------|-----|
| 1.25倍行距 | `Line="300" LineRule=Auto` |
| 段前/段后1行(小四) | `Before="240"` / `After="240"` |
| 段前/段后1行(三号) | `Before="320"` / `After="320"` |
| 首行缩进2字符 | `FirstLineChars="200"` |
| 悬挂缩进2字符 | `Hanging="480"` |

### 三线表边框（单位: 1/8 pt）
| 线 | Size | 说明 |
|----|------|------|
| 顶线 | 12 | 1.5pt 粗线 |
| 栏目线 | 12 | 1.5pt 粗线（表头单元格底部） |
| 底线 | 12 | 1.5pt 粗线 |
| 左右竖线 | None | 无 |
| 内部横线 | None | 无 |

### 图片尺寸
| 单位 | 换算 |
|------|------|
| 1 inch | 914400 EMU |
| 1 cm | 360000 EMU |
| 1 px@96dpi | 9525 EMU |

## 脚注实现要点

1. **必须先初始化 FootnotesPart**，包含 id=-1（分隔线）和 id=0（跨页延续线）的特殊脚注
2. **正文引用**：在段落末尾添加 `FootnoteReference`
3. **脚注内容**：在 FootnotesPart 中添加 `Footnote` 元素
4. **每页重新编号**：在 sectPr 中添加 `FootnoteProperties` + `NumberingRestart = EachPage`
5. **脚注位置**：中文规范中脚注标号放在句号之前（不同于英文习惯）

## C# 源码编码策略（2026-05-20 新增）

1. **文件编码**: 使用无 BOM 的 UTF-8（`new UTF8Encoding(false)`）
2. **避免直接出现的 Unicode 字符**: U+201C (`"`)、U+201D (`"`)、U+2014 (`—`)、U+2019 (`'`) 应替换为 `\uXXXX` 转义序列或直接删除
3. **Drawing 命名空间**: 必须使用别名 `A`/`DW`/`PIC`，不能直接 `using`
4. **测试先行**: 用最小测试文件验证编译工具链能正确处理中文字符后再编写完整代码

## 样式策略选择

本方案采用 **直接格式化**（direct formatting）而非 **样式引用**（style reference）：

- **优点**：完全控制每个段落的格式，不依赖样式继承链
- **缺点**：用户无法通过修改样式批量调整格式
- **折中**：定义了完整样式集（Heading1/2/3、TOC1/2、FootnoteText 等），但段落使用直接属性。如需切换为样式引用，只需将 `MakePP(just:"center",...)` 替换为 `MakePP(styleId:"Heading1")`

## 运行环境

- .NET SDK 8.0.421（路径: `C:\Users\ban\AppData\Local\Microsoft\dotnet\`）
- DocumentFormat.OpenXml 3.2.0
- PowerShell 中需先设置 `$env:Path` 包含 dotnet 路径
